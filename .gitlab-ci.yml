before_script:
  - apt-get update -qq && apt-get install -y -qq sudo make git

init_alpha:
  - make alpha-init
  - gem install bundler --no-ri --no-rdoc
  - bundle install --jobs $(nproc)  "${FLAGS[@]}"

test_alpha:
  - make invade
  - echo TODO