# wpa.conf
#
# wpa_supplicant(8) configuration template to be used during
# initial setup.
#
# use:
#   vi lib/wpa.conf
#   /sbin/wpa_supplicant -i wlp2s0 -c lib/wpa.conf &
#   /sbin/dhclient -i wlp2s0

network={
	ssid="xxx"
	key_mgmt=WPA-PSK
	psk="xxx"
}
