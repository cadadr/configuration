# i3 configuration

set $mod Mod4
font pango:DejaVu Sans 8
floating_modifier $mod

set $mode_run (1) PCManFM (2) Firefox (3) pass (4) xtype
mode "$mode_run" {
    bindsym 1 mode "default"; exec --no-startup-id pcmanfm
    bindsym 2 mode "default"; exec --no-startup-id ffprofile.sh
    bindsym 3 mode "default"; exec --no-startup-id passmenu.sh
    bindsym 3 mode "default"; exec --no-startup-id xtype.sh

    # exit mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym Control+g mode "default"
}
# Declare here the shortcut to bring the display selection menu
bindsym $mod+r mode "$mode_run"


bindsym F12 exec --no-startup-id i3-dmenu-desktop
bindsym Shift+F12 exec dmenu_run
bindsym $mod+Return exec emacsclient -c
bindsym $mod+Shift+Return exec i3-sensible-terminal
bindsym $mod+h exec --no-startup-id sh $MYLIB/i3cheatsheet.sh

# Set brightness
bindsym $mod+Shift+1 exec bright.rb 0.1
bindsym $mod+Shift+2 exec bright.rb 0.2
bindsym $mod+Shift+3 exec bright.rb 0.3
bindsym $mod+Shift+4 exec bright.rb 0.4
bindsym $mod+Shift+5 exec bright.rb 0.5
bindsym $mod+Shift+6 exec bright.rb 0.6
bindsym $mod+Shift+7 exec bright.rb 0.7
bindsym $mod+Shift+8 exec bright.rb 0.8
bindsym $mod+Shift+9 exec bright.rb 0.9
bindsym $mod+Shift+0 exec bright.rb 1

bindsym $mod+b focus left
bindsym $mod+n focus down
bindsym $mod+p focus up
bindsym $mod+f focus right

bindsym $mod+Shift+b move left
bindsym $mod+Shift+n move down
bindsym $mod+Shift+p move up
bindsym $mod+Shift+f move right

bindsym F11 fullscreen toggle
bindsym $mod+2 split v
bindsym $mod+3 split h

bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+1 layout toggle split
bindsym $mod+semicolon floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

bindsym $mod+u focus parent
bindsym $mod+c focus child

set $w1 "1/wrk"
set $w2 "2/www"

# switch to workspace
bindsym F1 workspace $w1
bindsym F2 workspace $w2
bindsym F3 workspace 3
bindsym F4 workspace 4
bindsym F5 workspace 5
bindsym F6 workspace 6
bindsym F7 workspace 7
bindsym F8 workspace 8
bindsym F9 workspace 9

# move focused container to workspace
bindsym Shift+F1 move container to workspace $w1
bindsym Shift+F2 move container to workspace $w2
bindsym Shift+F3 move container to workspace 3
bindsym Shift+F4 move container to workspace 4
bindsym Shift+F5 move container to workspace 5
bindsym Shift+F6 move container to workspace 6
bindsym Shift+F7 move container to workspace 7
bindsym Shift+F8 move container to workspace 8
bindsym Shift+F9 move container to workspace 9

bindsym $mod+g reload
bindsym $mod+Shift+g restart
bindsym $mod+Shift+e exec xterm -e 'less /run/user/$UID/i3/errorlog.*'
bindsym $mod+Ctrl+e exec xterm -e 'less ~/.xsession-errors'
bindsym $mod+e exec "i3-nagbar -t warning -m 'Bye?' -b 'Yes' 'i3-msg exit'"
bindsym $mod+k kill

# power management
# from: https://wiki.archlinux.org/index.php/I3
set $Locker lockscr.sh
bindsym $mod+l exec --no-startup-id $Locker, mode "default"
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # exit mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym Control+g mode "default"
}
bindsym Pause mode "$mode_system"

# manual management of external displays
# from: https://wiki.archlinux.org/index.php/I3
set $mode_display Ext Screen (h) HDMI ON, (H) HDMI OFF, (s) HDMI Sound ON, (S) HDMI Sound OFF
mode "$mode_display" {
    bindsym h exec --no-startup-id xrandr --output HDMI1 --auto --right-of LVDS1, mode "default"
    bindsym y exec --no-startup-id xrandr --output HDMI1 --auto --off, mode "default"
    bindsym s exec --no-startup-id pactl set-card-profile 0 output:hdmi-stereo
    bindsym S exec --no-startup-id pactl set-card-profile 0 output:analog-stereo

    # exit mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym Control+g mode "default"
}
# Declare here the shortcut to bring the display selection menu
bindsym F10 mode "$mode_display"

# color theme:
# colorclass		border	background	text 	indicator	child_border
client.focused		#0087af	#0087af		#ffffff	#0087af		#0087af
client.focused_inactive	#dedede	#dedede		#333333	#333333		#dedede
client.unfocused	#dddddd	#dddddd		#333333	#ffffff		#dedede
client.urgent		#333333	#ffffff		#C20000	#ffffff		#dedede

bar {
	font pango:DejaVu Sans 10
	position top
	status_command i3status
	colors {
		separator		#C20000
		background		#dddddd
		statusline		#000000
		focused_workspace	#ffffff	#333333	#ffffff
		active_workspace	#ffffff	#dddddd	#333333
		inactive_workspace	#dddddd	#dddddd	#333333
		urgent_workspace	#C20000	#dddddd	#333333
	}
}
